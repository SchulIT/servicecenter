{{ 'greeting'|trans({'%firstname%': user.firstname, '%lastname%': user.lastname }, 'mail') }}

{{ 'problem.comment.info'|trans({}, 'mail') }}

{{ 'problem.comment.link'|trans({'%url%': url('show_problem', { uuid: problem.uuid })}, 'mail') }}

{{ 'farewell'|trans({}, 'mail') }}

-----
{{ 'notice'|trans({}, 'mail') }}