{{ 'greeting'|trans({'%firstname%': user.firstname, '%lastname%': user.lastname }, 'mail') }}

{{ 'problem.updated.info'|trans({'%problem%': problem|problem }, 'mail') }}

{% for change in changes %}
* {{ change }}
{% endfor %}

{{ 'problem.updated.link'|trans({'%url%': link }, 'mail') }}

{{ 'farewell'|trans({}, 'mail') }}

-----
{{ 'notice'|trans({}, 'mail') }}