{{ 'greeting'|trans({'%firstname%': user.firstname, '%lastname%': user.lastname }, 'mail') }}

{{ 'problem.updated.info'|trans({}, 'mail') }}

{% for change in changes %}
* {{ change }}
{% endfor %}

{{ 'problem.updated.link'|trans({'%url%': url('show_problem', { uuid: problem.uuid })}, 'mail') }}

{{ 'farewell'|trans({}, 'mail') }}

-----
{{ 'notice'|trans({}, 'mail') }}